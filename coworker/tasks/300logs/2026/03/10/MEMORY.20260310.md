## Daily Memory - 2026-03-10

- **Coworker path handling centralized:** Replaced coworker PowerShell scripts' repo-root discovery (`git rev-parse`, ROOT/VERSION scans) and `Set-Location` usage with shared config-backed helpers in `coworker/scripts/config.ps1` + `config.psd1`. Scheduler and worker scripts now resolve workspace/task paths from script-relative configuration, while Copilot child processes receive an explicit working directory instead of mutating the caller's current directory. **Learning:** For coworker automation, script-relative resolution plus a PSD1-defined workspace root is more reliable than probing repo markers or changing global shell state. **Validation:** PowerShell syntax parsing passed for `coworker/scripts/**/*.ps1`, `git diff --check` passed, and a config smoke test resolved `WorkspaceRoot`, `TasksRoot`, and `SchedulerWorkingDirectory` to the expected absolute paths.

- **Scheduler async behavior preserved while fixing paths:** Kept `coworker-scheduler.ps1` launching queue processors with `Start-Process` and no wait, but moved its working-directory choice into configuration (`coworker-scheduler.config.psd1`). **Learning:** When throughput already depends on non-blocking scheduler dispatch, the safe improvement is to centralize cwd policy in config and leave worker polling semantics intact.

- **Scheduler placeholder-aware queue detection:** Updated coworker queue scanning to ignore placeholder files such as `.gitkeep` via shared helpers in `coworker/scripts/config.ps1`, then applied that filter in `coworker-scheduler.ps1`, `process-coworker-queue.ps1`, `process-draft-refinement-queue.ps1`, and `workers/refine-drafts.ps1`. Also fixed `coworker-scheduler.ps1 -Once` so skipped tasks with no `LastExitCode` are not misreported as failures. **Learning:** Empty-queue suppression has to treat repository placeholder files as non-work and align one-shot exit semantics with skipped runs, otherwise the scheduler still pops unnecessary child processes or reports false failures. **Validation:** `git diff --check` passed, all edited scripts parsed cleanly, a temporary scheduler smoke config confirmed placeholder-only queues stay `WaitingForWork` with `RunCount=0`, real files still launch workers, and draft refinement placeholder checks leave `.gitkeep` untouched.
